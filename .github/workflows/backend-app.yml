name: Backend App CI

on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '8.0.x'
    - run: dotnet restore ./src/backend-app
    - run: dotnet build --no-restore ./src/backend-app/FitnessTrackerSolution.sln
    - run: dotnet test ./src/backend-app